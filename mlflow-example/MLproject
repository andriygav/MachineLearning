name: example

docker_env:
   image: ap-res-docker-train
   
entry_points:
  main:
    parameters:
      C: {type: float, default: 1.0}
      penalty: {type: str, default: "l1"}
    command: "python3 train.py --C {C} --penalty {penalty}"
